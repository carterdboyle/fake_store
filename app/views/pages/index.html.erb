<div
  class="relative flex grow flex-col items-center justify-center text-center
         min-h-[60vh] bg-center bg-cover"
  style="background-image: url(<%= "/images/background.png" %>);"
>
  <!-- (optional) tint so text stays readable -->
  <div class="absolute inset-0 bg-white/90"></div>

  <div class="relative text-shadow-lg">
    <h1 class="text-6xl font-light tracking-widest">Welcome to the home page!</h1>
    <p class="text-xl mt-10">
      <%= link_to "View our products!", products_path, class: "hover:underline" %>
    </p>
  </div>
</div>